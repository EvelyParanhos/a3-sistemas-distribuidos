services:
  app:
    volumes:
      - ./app:/usr/src/app
      - /usr/src/app/node_modules
    command: node --watch index.js
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
